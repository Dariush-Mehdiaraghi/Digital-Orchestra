{
  "name": "webrtc_osc_project",
  "version": "1.0.0",
  "description": "Sending OSC via WebRTC",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babelify": "10.0.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.2",
    "peer": "^0.2.10",
    "ringbufferjs": "^1.1.0",
    "socket.io": "^2.3.0",
    "timesync": "^1.0.5"
  },
  "devDependencies": {},
  "browserify": {
    "transform": [
      "babelify"
    ]
  }
}
